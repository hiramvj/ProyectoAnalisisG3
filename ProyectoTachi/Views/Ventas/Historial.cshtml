@model List<Abstracciones.Modelos.PedidoVentaListadoDto>
@{
    ViewData["Title"] = "Historial de Ventas";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h2 class="mb-0">Historial de Ventas</h2>
        <small class="text-muted">Filtrá por fechas, cliente, estado o método de pago.</small>
    </div>

    <div>
        <a asp-action="ProductosMasVendidos" class="btn btn-outline-primary">
            <i class="bi bi-bar-chart-line me-1"></i> Productos Más Vendidos
        </a>

        <a asp-action="Index" class="btn btn-success">
            <i class="bi bi-plus-circle me-1"></i> Nuevo Pedido
        </a>
    </div>
</div>

<div class="card shadow-sm mb-3">
    <div class="card-body">
        <form method="get" asp-action="Historial">
            <div class="row g-3 align-items-end">

                <div class="col-lg-4">
                    <label class="form-label">Buscar</label>
                    <input class="form-control"
                           name="q"
                           value="@(ViewBag.Q ?? "")"
                           placeholder="Número, cliente, estado..." />
                </div>

                <div class="col-lg-2">
                    <label class="form-label">Desde</label>
                    <input type="date"
                           class="form-control"
                           name="desde"
                           value="@(ViewBag.Desde ?? "")" />
                </div>

                <div class="col-lg-2">
                    <label class="form-label">Hasta</label>
                    <input type="date"
                           class="form-control"
                           name="hasta"
                           value="@(ViewBag.Hasta ?? "")" />
                </div>

                <div class="col-lg-2">
                    <label class="form-label">Cliente</label>
                    <select name="clienteId"
                            class="form-select"
                            asp-items="ViewBag.Clientes">
                        <option value="">-- Todos --</option>
                    </select>
                </div>

                <div class="col-lg-2">
                    <label class="form-label">Método pago</label>
                    <select name="metodoPagoId"
                            class="form-select"
                            asp-items="ViewBag.MetodosPago">
                        <option value="">-- Todos --</option>
                    </select>
                </div>

                <div class="col-lg-2">
                    <label class="form-label">Estado</label>
                    <select name="estado" class="form-select">
                        <option value="">-- Todos --</option>
                        <option value="CREADA" selected="@(ViewBag.Estado == "CREADA")">CREADA</option>
                        <option value="ENTREGADA" selected="@(ViewBag.Estado == "ENTREGADA")">ENTREGADA</option>
                        <option value="CANCELADA" selected="@(ViewBag.Estado == "CANCELADA")">CANCELADA</option>
                    </select>
                </div>

                <div class="col-lg-1 d-grid">
                    <button class="btn btn-primary">
                        <i class="bi bi-funnel-fill"></i>
                    </button>
                </div>
            </div>

            <div class="mt-2">
                <a asp-action="Historial" class="btn btn-link p-0">
                    <i class="bi bi-x-circle me-1"></i> Limpiar filtros
                </a>
            </div>
        </form>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-striped mb-0">
            <thead>
                <tr>
                    <th style="width:90px;">#</th>
                    <th style="width:140px;">Fecha</th>
                    <th>Cliente</th>
                    <th style="width:180px;">Método Pago</th>
                    <th style="width:140px;">Estado</th>
                    <th style="width:180px;">Acciones</th>
                </tr>
            </thead>

            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var x in Model)
                    {
                        <tr>
                            <td class="fw-bold">@x.NumeroPedido</td>
                            <td>@x.FechaPedido.ToString("dd/MM/yyyy")</td>
                            <td>@x.ClienteNombre</td>
                            <td>@(string.IsNullOrEmpty(x.MetodoPagoNombre) ? "-" : x.MetodoPagoNombre)</td>
                            <td>
                                @if (x.Estado == "CREADA")
                                {
                                    <span class="badge bg-warning text-dark">CREADA</span>
                                }
                                else if (x.Estado == "ENTREGADA")
                                {
                                    <span class="badge bg-success">ENTREGADA</span>
                                }
                                else if (x.Estado == "CANCELADA")
                                {
                                    <span class="badge bg-danger">CANCELADA</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">@x.Estado</span>
                                }
                            </td>

                            <td>
                                <a asp-action="Detalle"
                                   asp-route-id="@x.PedidoVentaId"
                                   class="btn btn-sm btn-outline-secondary">
                                    Ver
                                </a>

                                <a asp-action="Editar"
                                   asp-route-id="@x.PedidoVentaId"
                                   class="btn btn-sm btn-warning">
                                    Editar
                                </a>

                                <a asp-action="NotaAjuste"
                                   asp-route-id="@x.PedidoVentaId"
                                   class="btn btn-sm btn-outline-warning"
                                   title="Ajuste Contable">
                                    <i class="bi bi-file-earmark-diff"></i>
                                </a>

                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6" class="text-center py-4 text-muted">
                            No hay registros que coincidan con los filtros aplicados.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>